package com.strings;

import java.util.Scanner;

public class StringComparisonClient {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*
		 * Scanner scanner = new Scanner(System.in);
		 * 
		 * int numStrings = StringComparison.getValidNumStrings(scanner);
		 * 
		 * String comparisonType = StringComparison.getValidComparisonType(scanner);
		 * 
		 * StringComparison.validateNumberOfStrings(numStrings, comparisonType);
		 */

        System.out.println(Math.min(Double.MIN_VALUE, 0.0d));
	}

}
